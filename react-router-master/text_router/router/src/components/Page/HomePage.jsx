import React from 'react'

export default function HomePage() {
  const pageContainerStyle = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    fontSize: "2rem",
    fontWeight: "bold",
  };

  return (
    <div style={pageContainerStyle}>HomePage</div>
  );
}